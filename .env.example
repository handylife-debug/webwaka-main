# WebWaka Environment Configuration
# Copy this file to .env.local and configure your actual values

# === DATABASE CONFIGURATION ===
# PostgreSQL database connection (provided by Replit automatically)
DATABASE_URL=postgresql://username:password@host:port/database
PGPORT=5432
PGUSER=replit
PGPASSWORD=password
PGDATABASE=main
PGHOST=localhost

# === REDIS CONFIGURATION ===
# Upstash Redis for tenant data storage and caching
KV_REST_API_URL=https://your-redis-instance.upstash.io
KV_REST_API_TOKEN=your-redis-token

# === DOMAIN CONFIGURATION ===
# Root domain for multi-tenant routing
NEXT_PUBLIC_ROOT_DOMAIN=localhost:3000
# For production: NEXT_PUBLIC_ROOT_DOMAIN=yourdomain.com

# === REPLIT ENVIRONMENT ===
# These are automatically provided by Replit
# REPL_ID=automatically-set
# REPL_SLUG=automatically-set
# REPLIT_DB_URL=automatically-set
# REPL_IDENTITY=automatically-set
# WEB_REPL_RENEWAL=automatically-set

# === CONNECTOR SETTINGS ===
# For Replit connectors (automatically configured)
# REPLIT_CONNECTORS_HOSTNAME=automatically-set
# REPL_IDENTITY=automatically-set

# === TWILIO CONFIGURATION (Optional) ===
# For SMS notifications via Twilio integration
# TWILIO_ACCOUNT_SID=your-account-sid
# TWILIO_AUTH_TOKEN=your-auth-token
# TWILIO_PHONE_NUMBER=your-twilio-phone

# === GITHUB INTEGRATION (Optional) ===
# Configured via Replit GitHub connector
# GITHUB_TOKEN=automatically-handled-by-connector

# === EMAIL CONFIGURATION (Optional) ===
# For email notifications via Replit Mail
# REPLIT_MAIL_KEY=automatically-handled-by-integration

# === PRODUCTION SETTINGS ===
# NODE_ENV=development
# For production deployment:
# NODE_ENV=production